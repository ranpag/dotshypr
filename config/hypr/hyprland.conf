# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/
#
# Please note not all available settings / options are set here.
# For a full list, see the wiki

###################
### MORE CONFIG ###
###################

source = ~/.config/hypr/hyprland/colors.conf
source = ~/.config/hypr/hyprland/run.conf
source = ~/.config/hypr/hyprland/env.conf
source = ~/.config/hypr/hyprland/input.conf
source = ~/.config/hypr/hyprland/look.conf


################
### MONITORS ###
################

monitor=,preferred,auto,1


#################
### AUTOSTART ###
#################

exec-once = swww-daemon
# exec-once = hypridle
exec-once = systemctl --user start hyprpolkitagent
exec-once = eww open-many bar dock


###################
### PERMISSIONS ###
###################

# ecosystem {
#   enforce_permissions = 1
# }

permission = /usr/(bin|local/bin)/grim, screencopy, allow
permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


##############################
### WINDOWS AND WORKSPACES ###
##############################

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

windowrule = rounding 5, class:^()$

# xdg-desktop-portal-*
windowrule = float,class:^(xdg-desktop-portal.*)$
windowrule = center (1),class:^(xdg-desktop-portal.*)$
windowrule = size <50% <50%,class:^(xdg-desktop-portal.*)$

# feh
windowrule = float, class:^(feh)$
windowrule = center (1),class:^(feh)$
windowrule = size >20% >20%,class:^(feh)$
windowrule = size <85% <85%,class:^(feh)$

# mpv
windowrule = float, class:^(mpv)$
windowrule = size >65% >65%,class:^(mpv)$
windowrule = size <85% <85%,class:^(mpv)$

# zathura
windowrule = float, class:^(org.pwmt.zathura)$
windowrule = center (1),class:^(org.pwmt.zathura)$
windowrule = opacity 0.98, class:^(org.pwmt.zathura)$
windowrule = size <70% 70%,class:^(org.pwmt.zathura)$

# xarchiver
windowrule = float, class:^(xarchiver)$
windowrule = center (1),class:^(xarchiver)$
windowrule = size <50% <70%,class:^(xarchiver)$

# terminal kitty|foot
windowrule = opacity 0.65, class:^(kitty|foot)$
